# Frontend Collaboration Notes

- 在 `LsERPPortal` 落前端代码时，必须注意合理拆分文件和文件夹。
- 新功能默认按职责分层组织，例如：`api`、`components`、`hooks`、`pages`、`types`、`utils`。
- 避免把页面、数据请求、状态处理、渲染细节全部堆到单一入口文件中。
- 优先保证目录结构清晰、命名稳定、后续可维护，再补充具体实现。
